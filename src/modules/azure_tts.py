# from aspeak import SpeechService, AudioFormat
# from os import getenv
# from dotenv import load_dotenv

# load_dotenv()

# REGION = getenv("AZURE_REGION")
# SPEECH_KEY = getenv("AZURE_SPEECH_KEY")

# tts = SpeechService(region=REGION, key=SPEECH_KEY)
# tts.connect()


# def generate_audio(text: str):
#     tts.speak_text(text=text, voice="en-US-AshleyNeural", rate="+10%", pitch="+25%")
